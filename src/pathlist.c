#include <stdio.h>
#include "csv.h"
#include "pathlist.h"

static Pathlist * format(char *);

void
pladd(Pathlist *list, char *template)
{
	//first, format the template to remove any format strings (like [CLASS])
	//Pathlist *paths = format(template);
}

static Pathlist *
format(char *template)
{
	return NULL;
}

